{
  "name": "wasm-federation-demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "lerna run start --stream",
    "build:wasm": "wasm-pack build --release ./packages/remote-wasm"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
